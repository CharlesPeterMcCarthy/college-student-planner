<Page x:Class="StudentPlanner.MyPlanner"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StudentPlanner"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MyPlanner">

    <Page.Resources>
        <DataTemplate x:Key="TaskTemplate">
            <StackPanel Name="task1" Background="LightGray">
                <TextBlock Text="{Binding Path=Title, Mode=OneWay}"/>
                <TextBlock Text="{Binding Path=Description, Mode=OneWay}"/>
                <TextBlock Text="{Binding Path=DueDatetime, Mode=OneWay}"/>
                <Label Content="Priority: " />
                <TextBlock Text="{Binding Path=Priority, Mode=OneWay}"/>
                <Label Content="Status: " />
                <TextBlock Text="{Binding Path=Status, Mode=OneWay}"/>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Grid Name="mainGrid" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition Width="80*"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Text="Monday" Grid.Row="0" Grid.Column="0" FontSize="20" FontWeight="bold" VerticalAlignment="Center" HorizontalAlignment="Center" />
        <TextBlock Text="Tuesday" Grid.Row="1" Grid.Column="0" FontSize="20" FontWeight="bold" VerticalAlignment="Center" HorizontalAlignment="Center" />
        <TextBlock Text="Wednesday" Grid.Row="2" Grid.Column="0" FontSize="20" FontWeight="bold" VerticalAlignment="Center" HorizontalAlignment="Center" />
        <TextBlock Text="Thursday" Grid.Row="3" Grid.Column="0" FontSize="20" FontWeight="bold" VerticalAlignment="Center" HorizontalAlignment="Center" />
        <TextBlock Text="Friday" Grid.Row="4" Grid.Column="0" FontSize="20" FontWeight="bold" VerticalAlignment="Center" HorizontalAlignment="Center" />
        <TextBlock Text="Saturday" Grid.Row="5" Grid.Column="0" FontSize="20" FontWeight="bold" VerticalAlignment="Center" HorizontalAlignment="Center" />
        <TextBlock Text="Sunday" Grid.Row="6" Grid.Column="0" FontSize="20" FontWeight="bold" VerticalAlignment="Center" HorizontalAlignment="Center" />

        <ListBox Grid.Row="0" Grid.Column="1" Name="monList" ItemTemplate="{StaticResource TaskTemplate}" SelectionChanged="DayList_SelectionChanged" />
        <ListBox Grid.Row="1" Grid.Column="1" Name="tuesList" ItemTemplate="{StaticResource TaskTemplate}" SelectionChanged="DayList_SelectionChanged" />
        <ListBox Grid.Row="2" Grid.Column="1" Name="wedList" ItemTemplate="{StaticResource TaskTemplate}" SelectionChanged="DayList_SelectionChanged" />
        <ListBox Grid.Row="3" Grid.Column="1" Name="thurList" ItemTemplate="{StaticResource TaskTemplate}" SelectionChanged="DayList_SelectionChanged" />
        <ListBox Grid.Row="4" Grid.Column="1" Name="friList" ItemTemplate="{StaticResource TaskTemplate}" SelectionChanged="DayList_SelectionChanged" />
        <ListBox Grid.Row="5" Grid.Column="1" Name="satList" ItemTemplate="{StaticResource TaskTemplate}" SelectionChanged="DayList_SelectionChanged" />
        <ListBox Grid.Row="6" Grid.Column="1" Name="sunList" ItemTemplate="{StaticResource TaskTemplate}" SelectionChanged="DayList_SelectionChanged" />

    </Grid>
</Page>
