<Page x:Class="StudentPlanner.ViewTask"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StudentPlanner"
      xmlns:models="clr-namespace:StudentPlanner.Models"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ViewTask">

    <Page.Resources>
        <Style x:Key="LabelText" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource MaterialDesignSubheadingTextBlock}">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="Padding" Value="0, 10, 40, 0" />
        </Style>

        <Style x:Key="EditValue" TargetType="{x:Type TextBox}" BasedOn="{StaticResource MaterialDesignFloatingHintTextBox}">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Padding" Value="40, 0, 0, 0" />
            <Setter Property="FontSize" Value="18" />
        </Style>

        <Style x:Key="ActionButton" TargetType="{x:Type Button}" BasedOn="{StaticResource MaterialDesignRaisedAccentButton}">
            <Setter Property="Margin" Value="0, 0, 20, 0" />
            <Setter Property="Width" Value="160" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </Page.Resources>

    <Grid Background="White" Name="taskGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40*"/>
                <ColumnDefinition Width="60*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Title" Style="{StaticResource LabelText}" />
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path=Title, Mode=TwoWay}" Style="{StaticResource EditValue}" />
            
            <Separator Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource MaterialDesignDarkSeparator}" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Description" Style="{StaticResource LabelText}" />
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Path=Description, Mode=TwoWay}" Style="{StaticResource EditValue}" />

            <Separator Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource MaterialDesignDarkSeparator}" />

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Due Date" Style="{StaticResource LabelText}" />
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Path=DueDatetime, Mode=TwoWay}" Style="{StaticResource EditValue}" />

            <Separator Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource MaterialDesignDarkSeparator}" />

            <TextBlock Grid.Row="6" Grid.Column="0" Text="Priority" Style="{StaticResource LabelText}" />
            <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding Path=Priority, Mode=TwoWay}" Style="{StaticResource EditValue}" />

            <Separator Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource MaterialDesignDarkSeparator}" />

            <TextBlock Grid.Row="8" Grid.Column="0" Text="Status" Style="{StaticResource LabelText}" />
            <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding Path=Status, Mode=TwoWay}" Style="{StaticResource EditValue}" />

            <Separator Grid.Row="9" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource MaterialDesignDarkSeparator}" />
        </Grid>

        <ContentControl Name="taskInfo" Grid.Row="1">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type models:ExamTask}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40*"/>
                            <ColumnDefinition Width="60*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Subject" Style="{StaticResource LabelText}" />
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path=Subject, Mode=TwoWay}" Style="{StaticResource EditValue}" />

                        <Separator Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource MaterialDesignDarkSeparator}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Percentage" Style="{StaticResource LabelText}" />
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Path=PercentageWorth, Mode=TwoWay}" Style="{StaticResource EditValue}" />

                        <Separator Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource MaterialDesignDarkSeparator}" />

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Materials Needed" Style="{StaticResource LabelText}" />
                        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Path=Materials, Mode=TwoWay}" Style="{StaticResource EditValue}" />

                        <Separator Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource MaterialDesignDarkSeparator}" />
                    </Grid>
                </DataTemplate>
                <DataTemplate DataType="{x:Type models:AssignmentTask}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40*"/>
                            <ColumnDefinition Width="60*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Subject" Style="{StaticResource LabelText}" />
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path=Subject, Mode=TwoWay}" Style="{StaticResource EditValue}" />

                        <Separator Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource MaterialDesignDarkSeparator}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Percentage" Style="{StaticResource LabelText}" />
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Path=PercentageWorth, Mode=TwoWay}" Style="{StaticResource EditValue}" />
                        
                        <Separator Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource MaterialDesignDarkSeparator}" />
                    </Grid>
                </DataTemplate>
                <DataTemplate DataType="{x:Type models:EventTask}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40*"/>
                            <ColumnDefinition Width="60*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Location" Style="{StaticResource LabelText}" />
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path=Location, Mode=TwoWay}" Style="{StaticResource EditValue}" />

                        <Separator Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource MaterialDesignDarkSeparator}" />
                    </Grid>
                </DataTemplate>
                <DataTemplate DataType="{x:Type models:PaymentTask}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40*"/>
                            <ColumnDefinition Width="60*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Amount" Style="{StaticResource LabelText}" />
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path=Amount, Mode=TwoWay}" Style="{StaticResource EditValue}" />

                        <Separator Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource MaterialDesignDarkSeparator}" />
                    </Grid>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>

        <Grid Grid.Row="2" HorizontalAlignment="Right" Margin="0, 40, 0, 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            
            <ContentControl Name="btnStart" Grid.Column="0">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type models:ExamTask}">
                        <Button Content="Start Exam" Click="StartTask" Style="{StaticResource ActionButton}" />
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type models:AssignmentTask}">
                        <Button Content="Start Assignment" Click="StartTask" Style="{StaticResource ActionButton}" />
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type models:EventTask}">
                        <Button Content="Start Event" Click="StartTask" Style="{StaticResource ActionButton}" />
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type models:PaymentTask}" />
                </ContentControl.Resources>
            </ContentControl>

            <ContentControl Name="btnPause" Grid.Column="0">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type models:ExamTask}" />
                    <DataTemplate DataType="{x:Type models:AssignmentTask}">
                        <Button Content="Pause Assignment" Click="PauseTask" Style="{StaticResource ActionButton}" />
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type models:EventTask}">
                        <Button Content="Pause Event" Click="PauseTask" Style="{StaticResource ActionButton}" />
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type models:PaymentTask}" />
                </ContentControl.Resources>
            </ContentControl>

            <Button Name="completeButton" Grid.Column="1" Content="Complete" Click="CompleteTask" Style="{StaticResource ActionButton}" />
        </Grid>
    </Grid>
</Page>
