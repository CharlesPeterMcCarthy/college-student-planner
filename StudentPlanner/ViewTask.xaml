<Page x:Class="StudentPlanner.ViewTask"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StudentPlanner"
      xmlns:models="clr-namespace:StudentPlanner.Models"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ViewTask">

    <Grid Background="White" Name="taskGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition Width="80*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Title" />
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Path=Title, Mode=OneWay}" FontWeight="Bold" VerticalAlignment="Center" />
            <Label Grid.Row="1" Grid.Column="0" Content="Description" />
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Path=Description, Mode=OneWay}" VerticalAlignment="Center" />
            <Label Grid.Row="2" Grid.Column="0" Content="Due" />
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Path=DueDatetime, Mode=OneWay}" VerticalAlignment="Center" />
            <Label Grid.Row="3" Grid.Column="0" Content="Priority" />
            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Path=Priority, Mode=OneWay}" VerticalAlignment="Center" />
            <Label Grid.Row="4" Grid.Column="0" Content="Status" />
            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Path=Status, Mode=OneWay}" VerticalAlignment="Center" />
        </Grid>

        <ContentControl Name="btnStart" Grid.Row="1">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type models:ExamTask}">
                    <Button Content="Start Exam" Click="StartTask" />
                </DataTemplate>
                <DataTemplate DataType="{x:Type models:AssignmentTask}">
                    <Button Content="Start Assignment" Click="StartTask" />
                </DataTemplate>
                <DataTemplate DataType="{x:Type models:EventTask}">
                    <Button Content="Start Event" Click="StartTask" />
                </DataTemplate>
                <DataTemplate DataType="{x:Type models:PaymentTask}" />
            </ContentControl.Resources>
        </ContentControl>

        <ContentControl Name="btnPause" Grid.Row="2">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type models:ExamTask}" />
                <DataTemplate DataType="{x:Type models:AssignmentTask}">
                    <Button Content="Pause Assignment" Click="PauseTask" />
                </DataTemplate>
                <DataTemplate DataType="{x:Type models:EventTask}">
                    <Button Content="Pause Event" Click="PauseTask" />
                </DataTemplate>
                <DataTemplate DataType="{x:Type models:PaymentTask}" />
            </ContentControl.Resources>
        </ContentControl>

        <Button Name="completeButton" Grid.Row="3" Content="Complete" Click="Button_Click" />
    </Grid>
</Page>
