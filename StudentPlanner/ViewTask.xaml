<Page x:Class="StudentPlanner.ViewTask"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StudentPlanner"
      xmlns:models="clr-namespace:StudentPlanner.Models"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ViewTask">

    <Grid Background="White" Name="taskGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition Width="80*"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Row="0" Grid.Column="0">
            <TextBlock Text="{Binding Path=Title, Mode=OneWay}"/>
            <TextBlock Text="{Binding Path=Description, Mode=OneWay}"/>
            <TextBlock Text="{Binding Path=DueDatetime, Mode=OneWay}"/>
            <Label Content="Priority: " />
            <TextBlock Text="{Binding Path=Priority, Mode=OneWay}"/>
            <Label Content="Status: " />
            <TextBlock Text="{Binding Path=Status, Mode=OneWay}"/>
        </StackPanel>

        <ContentControl Name="content1" Grid.Row="1" Grid.Column="1">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type models:ExamTask}">
                    <StackPanel>
                        <Label Content="Exam Task"></Label>
                        <Button Content="Start Exam" Click="StartTask" />
                    </StackPanel>
                </DataTemplate>
                <DataTemplate DataType="{x:Type models:AssignmentTask}">
                    <StackPanel>
                        <Label Content="Assignment Task"></Label>
                        <Button Content="Start Assignment" Click="StartTask" />
                        <Button Content="Pause Assignment" Click="PauseTask" />
                    </StackPanel>
                </DataTemplate>
                <DataTemplate DataType="{x:Type models:EventTask}">
                    <StackPanel>
                        <Label Content="Event Task"></Label>
                        <Button Content="Start Event" Click="StartTask" />
                        <Button Content="Pause Event" Click="PauseTask" />
                    </StackPanel>
                </DataTemplate>
                <DataTemplate DataType="{x:Type models:PaymentTask}">
                    <StackPanel>
                        <Label Content="Payment Task"></Label>
                    </StackPanel>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
        
        <Button Grid.Row="2" Grid.Column="1" Content="Complete" Click="Button_Click" />
    </Grid>
</Page>
